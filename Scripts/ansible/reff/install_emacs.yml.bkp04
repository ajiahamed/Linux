---

- hosts: rpi
  become: true
  tasks:

  - name: install apache, php
    package:
      name: 
        - "{{ apache_package }}"
        - "{{ php_package }}"
      state: latest
      update_cache: yes
